// Basic setup for a typescript project

// Create a new project with npm init -y
// Install typescript as a dev dependency with npm install typescript --save-dev
// Create a tsconfig.json file with npx tsc --init
// Compile the typescript file with npx tsc 0-basicset.ts
// Run the compiled javascript file with node 0-basicset.js

// This is a basic setup for a typescript project. You can add more files and compile them together with npx tsc. You can also use a build tool like webpack or rollup to bundle your files together.